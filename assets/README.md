# Assets

Drop three screenshots here (the README references them by these exact filenames):

| File | What to capture | Tip |
|---|---|---|
| `workflow-diagram.png` | The n8n canvas with the 7 sticky-note stages visible | Zoom to fit, then screenshot; the labeled stages make it self-explanatory |
| `email-dashboard.png` | The dashboard summary email | Use a real run's email, or a dummy-data one |
| `sample-resume.png` | A generated resume (Doc or PDF, first page) | **Use dummy data** ; don't publish personal contact details |

Guidelines:
- Prefer **1200–1600px wide** PNGs; they render crisply on GitHub.
- **Scrub personal data** (real phone/email/address) before committing ; use the example profile to generate a demo resume for the screenshot.
- Keep file sizes reasonable (< ~500 KB each); compress if needed.

Until you add them, the README image links will show as broken ; that's expected.
